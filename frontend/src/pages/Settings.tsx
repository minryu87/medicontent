import React from 'react';

const Settings: React.FC = () => {
  // 추후 계정, 병원, 사용자, 통합, 보안 등 설정 구현
  return (
    <div>
      <h1>설정</h1>
      <p>계정, 병원, 사용자, 통합, 보안 등 설정 페이지</p>
    </div>
  );
};

export default Settings;
